/*
let p1 = prompt("Enter a password: ")
let p2 = prompt("Enter the second password: ")

function compare(pass1, pass2) {
  let equal = pass1.localeCompare(pass2)
  return (equal == 0) ? "The two passwords are the same": "The two passwords are not the same."
}


console.log(compare(p1, p2))
*/